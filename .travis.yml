env:
  global:

language: node_js

node_js:
- node
- lts/*

cache:
  directories:
  - node_modules

notifications:
  email: false

install: yarn

script: yarn build

deploy:
  provider: script
  script: bash .github/sftp-upload.sh
  on:
    branch: master